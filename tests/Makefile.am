# so that others can always use += for these variables
CLEANFILES =
TESTS =
EXTRA_DIST =

# TODO: Add manually created tests that should quit with over/underflow errors:
# int-max64p1-N5-parser.tcase: 2^64 or "maximum 64 bit unsigned plus 1")
# int-max65p1-N5-parser.tcase: 2^65 or "maximum 65 bit unsigned plus 1")
# int-max2Mp1-N5-parser.tcase: a test cases that overflows 2^M (but not I!).
# int-empty-N?-parser.tcase: empty input (zero bytes)
# int-X-head2-N?-parser.tcase: truncated one-byte input where X needed 2 bytes)
# int-Y-tail3-N?-parser.tcase: truncated two-byte input where Y needed 3 bytes)
TESTS +=

TEST_EXTENSIONS = .tcase
TCASE_LOG_COMPILER = $(SHELL) $(top_srcdir)/tests/run-tcase.sh
AM_TCASE_LOG_FLAGS = $(top_builddir)/src/HttpPaxxer/

dist_check_SCRIPTS = \
	run-tcase.sh

# Not distributed; XXX: listing it here breaks bootstrap or "make distcheck".
# generate-tcases.pl

# add test cases and supplementary files generated by make-tcases.pl
include $(top_srcdir)/tests/tcases/generated.am
